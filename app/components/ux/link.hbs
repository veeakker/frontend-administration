{{#if this.kindHasModel}}
  <LinkTo @route={{this.route}} @model={{@model}} ...attributes>
    {{this.label}}
  </LinkTo>
{{else}}
  <LinkTo @route={{this.route}} ...attributes>{{this.label}}</LinkTo>
{{/if}}