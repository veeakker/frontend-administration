{{#if this.kindHasModel}}
  <LinkTo @route={{this.route}} @model={{@model}} ...attributes>
    {{get @model this.linkProperty}}
  </LinkTo>
{{else}}
  <LinkTo @route={{this.route}} ...attributes></LinkTo>
{{/if}}