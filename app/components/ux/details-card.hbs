<Ui::Card ...attributes>
  <:title>
    {{#if (has-block "title")}}
      {{yield to="title"}}
    {{else}}
      {{@title}}
    {{/if}}
  </:title>
  <:body>
    {{#if (has-block "body")}}
      {{yield (hash
          Dt=(component "ux/details-card/dt")
          Dd=(component "ux/details-card/dd")
          Property=(component "ux/details-card/property" resource=@resource editable=@editable))
          to="body"}}
    {{else}}
      {{yield (hash
          Dt=(component "ux/details-card/dt")
          Dd=(component "ux/details-card/dd")
          Property=(component "ux/details-card/property" resource=@resource editable=@editable))
          to="default"}}
    {{/if}}
  </:body>
</Ui::Card>
