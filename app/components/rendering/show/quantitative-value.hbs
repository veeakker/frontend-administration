<span {{on "click" @startEditing}}>
  {{#let
       (get (get @resource @property) "unit")
       (get (get @resource @property) "value")
       as |unit value|}}
    {{value}} {{human-unit unit}}
  {{/let}}
</span>