<span class="input-group">
  <input {{on "input" (e/pipe (e/date-val) (e/set-prop @resource @property))}}
    class="form-control"
    type="date"
    value={{this.dateString}} />
  <BsButton
    {{on "click" @exitEditing}}
    @type="primary">
    <i class="bi bi-check" />
  </BsButton>
</span>