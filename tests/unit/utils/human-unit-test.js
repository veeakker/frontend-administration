import humanUnit from 'freddie/utils/human-unit';
import { module, test } from 'qunit';

module('Unit | Utility | human-unit', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = humanUnit();
    assert.ok(result);
  });
});
